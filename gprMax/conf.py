# Global configuration options for the simulation

# Store the current along the transmission line for each timestep
store_total_I_line = True

# Store the voltage along the transmission line for each timestep
store_total_V_line = True
